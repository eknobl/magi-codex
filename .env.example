# Database — Vercel Postgres or any PostgreSQL connection string
DATABASE_URL=postgres://user:password@host:5432/magi_codex?sslmode=require

# Anthropic API key for dispatch generation
ANTHROPIC_API_KEY=sk-ant-...

# Simple secret to protect the author dashboard (checked in middleware)
DASHBOARD_SECRET=change-this-to-a-random-string
